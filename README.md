# smart_pgmanagement_railway

Smart PG Management System configured for deployment on Railway using a unified Docker setup (Tomcat + MySQL in one container) to prevent the need for any code changes to the database connections.
